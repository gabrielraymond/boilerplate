import React from "react";
import FormLogin from "./form";

const Login = () => {
  return (
    <div className="w-screen h-[calc(100vh-72px)] bg-primary flex justify-center items-center">
      <FormLogin />
    </div>
  );
};

export default Login;
